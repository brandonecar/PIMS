"""Helpers for putting together the refinery LP input data."""


def build_model_inputs(case_data: dict) -> dict:
    """Return the case data in one place before the solver is added."""
    return case_data
